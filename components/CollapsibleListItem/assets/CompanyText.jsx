import styled from "styled-components";
import { motion } from "framer-motion";

const CompanyText = styled(motion.p)`
  font-weight: 800;
  font-size: 1.6rem;
  color: black;
  margin: 0;
`;

export default CompanyText;

import styled from "styled-components";
import { motion } from "framer-motion";

const RightIcon = styled(motion.span)`
  font-weight: 200;
  font-size: 2rem;
  color: black;
  margin: 0;
`;

export default RightIcon;

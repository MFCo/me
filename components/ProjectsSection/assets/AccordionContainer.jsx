import styled from "styled-components";
import { motion } from "framer-motion";

const AccordionContainer = styled(motion.ul)`
  list-style: none;
  margin: 0;
  padding: 0;
  width: 100%;
`;

export default AccordionContainer;
